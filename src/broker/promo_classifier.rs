//! Superseded by `crate::spam_filter::classifier` (rule-aware
//! version with tenant-customizable thresholds + allow/block
//! lists). Kept as an empty file to avoid breaking the
//! workspace tree until a maintenance commit removes it.

#![allow(dead_code)]
